#include <stdio.h> 

int main() { 
  
int num1, num2, num3; 
printf("Inserisci tre numeri: "); 
scanf("%d %d %d", &num1, &num2, &num3); 

if ((num2 - num1) == (num3 - num2)) 
    { printf("I numeri sono in ordine.\n"); } 

else { printf("I numeri non sono in ordine.\n"); } 


return 0; 


    }



#include <stdio.h>

int main () {

    int Num1,Num2,Num3;
    printf("Inserisci tre numer: ");
    scanf("%d%d%d", &Num1,&Num2,&Num3);

    if else (Num2-Num1) == (Num3-Num2)
        
}