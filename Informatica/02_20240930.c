#include <stdio.h>
int main() {
    int A=0;
    int B=0;
    int C=0;
        printf("inserisci il primo valore");
        scanf("%d",&A);
        printf("inserisci il secondo valore");
        scanf("%d",&B);

    if (A==B){
        printf("il primo valore %d e uguale %d",A,B);

    }
    else{
        if (A>B);
        printf("A%d e maggiore di B:%d",A,B);
        else
        printf("A%d non e maggiore di B",A,B)
       
      
        

    }
  return 0;  
}